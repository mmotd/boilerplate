{
  "name": "one-hour-test",
  "version": "1.0.0",
  "description": "one hour test launch",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4",
    "firebase": "^2.4.0"
  },
  "devDependencies": {
    "browserify": "^13.0.0"
  }
}
